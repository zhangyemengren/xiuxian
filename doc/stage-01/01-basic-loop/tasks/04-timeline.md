# 任务 04：实现时间线容器

## 状态

- 状态：待开始
- 所属步骤：01 基础循环
- 前置任务：任务 03

## 目标结果

时间线能够按插入顺序保存多个 `TimelineEvent`，并允许测试读取记录。

## Rust 学习点

- 学习 `Vec<T>` 的创建、追加、长度和只读借用。

## 开始前阅读

- 任务 03 完成后的 `src/time_line/`

## 实现步骤

- [ ] 定义 `Timeline`，内部持有 `Vec<TimelineEvent>`。
- [ ] 实现空时间线构造函数。
- [ ] 实现追加事件的方法。
- [ ] 实现只读获取全部事件的方法。
- [ ] 添加测试：新时间线长度为 `0`。
- [ ] 添加测试：依次追加两个事件后顺序不变。

## 文件范围

- 允许修改：`src/time_line/`。
- 暂不修改：模拟器和 `App`，不在容器内部打印日志。

## 验证命令

```bash
cargo fmt --check
cargo test time_line
```

## 完成标准

- [ ] 时间线只保存事件，不负责推进回合。
- [ ] 调用方只能只读查看内部记录。
- [ ] 空集合和顺序测试通过。

## 实际结果

待完成后填写。
