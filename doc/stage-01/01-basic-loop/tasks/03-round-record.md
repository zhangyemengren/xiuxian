# 任务 03：定义回合记录

## 状态

- 状态：待开始
- 所属步骤：01 基础循环
- 前置任务：任务 02

## 目标结果

用结构化类型表示“第几个回合开始或结束”，后续日志不依赖散落的字符串拼接。

## Rust 学习点

- 学习枚举及使用 `match` 区分不同事件。

## 开始前阅读

- `src/time_line/mod.rs`
- `src/time_line/time.rs`
- [第一阶段说明](../../README.md)

## 实现步骤

- [ ] 在 `time_line` 模块中定义 `TimelineEvent` 枚举。
- [ ] 只加入 `RoundStarted { round: u32 }` 和 `RoundFinished { round: u32 }`。
- [ ] 为事件实现文本格式化，输出中必须包含回合编号。
- [ ] 添加测试：两种事件都能输出预期文本。
- [ ] 保留现有 `Time` 类型，不在本任务决定是否删除。

## 文件范围

- 允许修改：`src/time_line/mod.rs`，必要时新增一个专用文件。
- 暂不修改：模拟循环、`App`、角色和世界时间。

## 验证命令

```bash
cargo fmt --check
cargo test time_line
```

## 完成标准

- [ ] 两种回合事件是结构化数据。
- [ ] 格式化逻辑只有一处。
- [ ] 测试不依赖控制台输出。

## 实际结果

待完成后填写。
