# 任务 06：接入 App 并输出

## 状态

- 状态：待开始
- 所属步骤：01 基础循环
- 前置任务：任务 05

## 目标结果

运行 `cargo run` 时，`App` 会创建固定回合数的配置、执行模拟并按顺序输出时间线。

## Rust 学习点

- 学习模块调用和使用 `for` 遍历只读集合。

## 开始前阅读

- `src/main.rs`
- `src/app.rs`
- 任务 05 完成后的模拟器公开接口

## 实现步骤

- [ ] 在 `App::run` 中创建 `SimulationConfig`。
- [ ] 暂时使用一个容易人工检查的小回合数，例如 `3`。
- [ ] 创建并执行 `Simulation`。
- [ ] 遍历返回的时间线并打印每条事件。
- [ ] 模拟结束后输出总回合数。
- [ ] 人工检查输出顺序：开始 1、结束 1、开始 2、结束 2。

## 文件范围

- 允许修改：`src/app.rs`，必要时调整公开导出。
- 暂不修改：命令行参数、配置文件、角色和玩法规则。

## 验证命令

```bash
cargo fmt --check
cargo test
cargo run
```

## 完成标准

- [ ] `cargo run` 的输出顺序与实际执行顺序一致。
- [ ] `App` 只负责组装和输出，不包含回合循环。
- [ ] 程序正常退出。

## 实际结果

待完成后填写。
